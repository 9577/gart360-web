.row
  .col-md-8
    %section.panel.panel-default
      %header.panel-heading wiki分类

      .panel-body
        = simple_form_for resource, url: resource_path(resource), html: { class: 'form-horizontal row-border' } do |f|
          = f.input :name, autofocus: true

          .form-actions
            = f.button :submit, class: 'btn btn-info btn-large pull-right'

  .col-md-4
    %section.panel.panel-default
      %header.panel-heading.font-bold
        %i.fa.fa-lightbulb-o
        帮助中心
      .panel-body
        %p 这里添加一些帮助性的文字