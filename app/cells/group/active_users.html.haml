.group-focus.cf
  %span.aside-info
    %b.fl 小组活跃用户
  %ul
    - @users.each do |user|
      %li
        .con
          = link_to user_path(user), class: 'userpic' do
            = image_tag user.avatar_url
          %p
            = link_to user.username, user_path(user)